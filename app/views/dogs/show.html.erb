<h1><%= @dog.name %></h1>
<p>Breed: <%= @dog.breed %></p>
<p>Age: <%= @dog.age %></p>

<p>Employees:</p>

<ul>
    <% @employees.each do |employee| %>
        <li><%= "#{employee.first_name} #{employee.last_name}" %></li>
        <% end %>
</ul>
